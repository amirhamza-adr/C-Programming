Draw a flowchart and write a program that prints all the numbers from 10 to 1 in descending
order.

                     (FOR LOOP)
#include <stdio.h>

int main() 
{
    int i;
    for(i=10;i>=1; i--)
{
    if(i>1)
    {
        printf("%d,",i);
        
    }
    else 
    {
    printf("%d",i);
    
    }
}
    return 0;
}
