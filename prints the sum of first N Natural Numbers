write a c-program that prints the sum of first N Natural Numbers.

CODE(FOR LOOP)

#include<stdio.h>
int main()
{
    int number,sum=0;
    printf("Please Enter A Number:",number);
    scanf("%d",&number);   
    for(int i=0;i<=number;i++)
    {
        sum=sum+i;
    }
printf("The sum of first %d natural numbers is %d.",number,sum);
    
    return 0;
}
